# AGENTS.md

> 面向 AI 协作 Agent 与开发者的项目操作手册。**设计依据请阅读 `docs/system-design.md`（单一事实来源）**，本文件只回答"怎么协作、怎么跑、守什么规矩"。
> 行为准则参照 Claude 官方 CLAUDE.md 的防 LLM 编码失误指南改编，并融入本项目特定约定。

## 项目一句话

Care-LifeLine：基于 LangGraph 的企业级医疗智能体平台 —— 多 Agent 分级诊疗 + 质控 Agent（规则+LLM 双层把关）+ 数据闭环。目标：**可上线 MVP + 大厂 Agent 岗位面试作品**。

## 技术栈

- Python 3.13 + **uv**（唯一包管理工具，禁止 pip/poetry）
- LangGraph / LangChain（编排）
- FastAPI / Uvicorn（接入层，SSE 流式）
- PostgreSQL（会话 Checkpointer / 审计 / 反馈）+ Qdrant（指南向量检索）
- pytest / ruff / mypy / pre-commit（质量门禁）

## 常用命令（等价 `make <target>`，与 CI 同源）

```bash
make install          # uv sync
make dev              # 本地启动 API（uvicorn --reload）
make test             # 单元测试（tests/unit）
make eval             # 评测运行（tests/eval，输出报告）
make check            # 质量门禁 = lint + type + test
make compose-up       # docker compose 一键起依赖（postgres + qdrant + api）
```

## 目录（包级）

```
src/care_lifeline/
  api/        # FastAPI 路由/鉴权/限流/PHI 脱敏中间件
  graph/      # LangGraph 编排（builder + 节点按域分包）
  tools/      # Tool 协议与实现（FDA / RxNorm / 指南 RAG / OCR）
  memory/     # 患者纵向记忆
  proactive/  # 主动触发（事件/定时 + 分布式锁）
  safety/     # 质控规则引擎 + PHI 脱敏
  eval/       # 评测套件 / 数据集 / 指标
  audit/      # 审计日志
  config.py   # pydantic-settings
tests/
  unit/       # 单测（qc_rules 覆盖率 100% 门禁）
  eval/       # 评测运行
```

---

## 行为准则（所有 Agent / 协作者必须遵守）

> 通用准则偏谨慎，牺牲一点速度换取正确性。琐碎任务可用常识判断，不必逐条背诵。

### 1. 先思考再编码

- **显式陈述假设；不确定就问。** 不猜。
- 存在多种理解/方案时，**列出来让用户选**，不要默默替用户决定。
- 如果有更简单的做法，**直接说出来**；对过度设计可以提出异议。
- 有任何困惑立即停下，**说出困惑点，提问**，不带着困惑往下写。

### 2. 简单优先

- 用**最小代码**解决当前问题，不做投机式扩展（YAGNI）。
- 不加未被要求的功能、抽象、"灵活性"、配置项。
- 不为不可能发生的场景写错误处理。
- 能用 50 行解决就不要写 200 行。写完自问：**资深工程师会觉得这是过度设计吗？** 是就简化。

### 3. 外科手术式修改

- 只碰必须碰的代码；**不改动相邻代码、注释、格式**。
- 不重构没坏的东西；匹配现有风格（哪怕你更喜欢另一种写法）。
- 发现无关的死代码：**口头提出，不擅自删除**。
- 你的改动产生的孤儿（无用的 import/变量/函数）**必须清理**；既有死代码除非用户要求，否则不动。
- 检验标准：**每一行改动都能追溯到用户请求**。

### 4. 目标驱动执行

把任务转成可验证的目标，而不是"把它做好"：

- "加校验" → 先为非法输入写测试，再让测试通过
- "修 bug" → 先写复现测试，再修复
- "重构 X" → 保证重构前后测试全绿

多步任务先列简略计划：

```
1. [步骤] → 验证: [检查点]
2. [步骤] → 验证: [检查点]
```

**有效标准**：能独立循环推进、无需反复确认。无效标准（"让它能跑"）会导致频繁返工。

---

## Commit 规范（强制）

- **不主动 commit**：任何提交都须经用户明确同意。用户说"不要 commit"时绝不提交；"分阶段 commit + push"仅在用户授权后执行。
- **信息用中文**，格式：`<type>: <中文摘要>`，type 遵循 Conventional Commits：
  - `feat` 新功能 / `fix` 修复 / `refactor` 重构 / `docs` 文档 / `test` 测试 / `chore` 构建与工具 / `perf` 性能 / `style` 格式
  - 例：`feat: 新增质控规则引擎，支持版本化规则集`
- **批次合理**：一个 commit 只做一件逻辑内聚的事；不做巨型 commit，也不碎片化提交。同一功能的代码 + 测试 + 必要文档应同批提交。
- **量合理**：每个 commit 可独立 review、可回滚。提交前自查：是否只包含本批次的改动、有没有混入无关内容。
- 提交信息应说明"为什么"多于"改了什么"（如涉及，可在正文补充）。

---

## 重大决策记录（ADR，强制）

- **位置**：`docs/decisions/`。
- **何时写**：每次做出影响架构 / 接口 / 数据模型 / 关键流程的决策后，**必须在同一任务内**新增 ADR 文件，不拖延、不口头留存。
- **命名**：`NNNN-短横线主题.md`，编号递增不重用（如 `0001-持久化选型.md`）。
- **模板**：见 `docs/decisions/README.md`。保持精炼：状态 / 背景 / 决策 / 备选方案 / 后果 / 日期。
- 已有决策记录必须阅读后再做关联改动，避免推翻已确认的取舍。

---

## 学习笔记（docs/study/，面试弹药库）

> 与 ADR 的分工：ADR 记录"我们决定什么"（给协作者，防推翻）；study 记录"这个实现面试怎么讲"（给求职面试准备，防遗忘）。

- **位置**：`docs/study/`。
- **何时写**：完成一个功能 / 修复 / 优化后，若该实现有**面试讲述价值**（架构取舍、性能优化、工程技巧、踩坑教训、可量化的改进），必须在同一任务内写入一条学习笔记；没有价值则可不写。
- **命名**：`YYYY-MM-DD-主题.md`（如 `2026-08-27-流式输出与质控冲突.md`）。
- **内容**：模板见 `docs/study/README.md`。核心三要素：
  1. **背景与问题**：当时面临的真实问题（一句话）。
  2. **实现要点**：怎么做的、关键代码/架构、为什么这样做（权衡与备选）。
  3. **面试预演**：面试官可能追问什么、你该怎么答；尽量带数字或证据（"P95 从 X 降到 Y"）。
- 笔记要**可讲 30 秒-3 分钟**，服务"大厂 Agent 岗位面试"这一项目终极目标。

---

## 代码开发规范

### 耦合度

- 模块间**只通过稳定接口交互**（`Tool` 协议、统一 `AgentState`、公共类型）；禁止隐式全局状态、禁止跨层直接 import 内部实现。
- **依赖方向（单向）**：`api → graph → 能力层(tools / memory)`；`safety` / `audit` 为横切底座，被上层调用。禁止反向依赖。
- 新代码默认小、内聚、可单测。

### 代码文件拆分标准（达到任一即拆，**与文件长度无关**）

> 不以"文件太长"为由拆分，也不以"文件太短"为由合并——只以职责、复杂度、依赖边界为准。

1. **单一职责破坏**：一个文件承担 ≥2 个不相关职责 → 拆。
2. **圈复杂度**：单个函数圈复杂度 > 10 → 拆出子函数 / 子模块。
3. **参数过多**：函数参数 > 5，或参数构成明显数据簇 → 提取数据类（如 Pydantic 模型）。
4. **抽象层次混杂**：同一文件同时含协议、实现、适配器 → 按层次拆（如 `tools/base.py` 协议、`tools/fda.py` 实现）。
5. **依赖过重**：一个模块直接依赖的外部模块 > 8 个 → 审视边界，拆出共享层。
6. **测试失衡**：单测文件过大难以维护，或与实现规模严重失衡 → 按职责拆测试文件。

### 代码质量约束（强制）

**命名与结构**
- 命名语义化：变量/函数/类名说清"是什么/做什么"，禁止单字母命名（循环变量、`i/j/k` 除外）；布尔值用 `is_/has_/should_` 前缀。
- 一个函数只做一件事；函数体超过 30 行先自问能否拆出子函数。
- 公共接口（跨模块被调用）必须写 docstring：参数、返回、抛出的异常。

**类型与静态检查**
- 新代码**全量类型标注**（`mypy` 必须通过）；`safety/`、`api/`、`graph/` 为核心模块，标注从创建起就完整，不允许 `Any` 兜底。
- 对外返回一律用 Pydantic 模型 / dataclass 承载，禁止裸 dict 漂移。

**错误处理**
- 不吞异常：`except` 必须处理或显式记录（结构化日志带上下文），禁止 `except: pass` / 空 except。
- 不裸抛裸接：错误先转成领域语义（如 `QCBlockedError`），上层按类型处理。
- API 层错误统一结构 `{code, message, detail?}`，前端契约见 `docs/development-plan.md` §3。
- 日志：入口/出口、外部调用、异常三处必须留痕；日志不打印 PHI。

**测试质量**
- 每个行为变更必须配测试；断言**写具体值**（`assert x == 3`），禁止"断言非空/非 None"这类弱断言。
- 每个修复先写复现测试再修；回归用例进 `tests/` 后不得删除。
- 测试命名用 `test_行为_条件_期望`（如 `test_evaluate_紧急词_返回blocking`）。

**重复与依赖**
- 禁止复制粘贴式重复（DRY）：第三次出现同类代码时必须抽公共函数。
- 禁止重复造轮子：项目已有能力（`tools/base.py`、`safety/phi.py`、`audit/logger.py`）一律复用。
- 魔法数字/字符串提取为命名常量或配置（如规则阈值进 `config.py`）。

**性能与资源**
- IO 一律走异步（async/await 或线程池）；禁止在请求热路径同步阻塞。
- 数据库查询注意 N+1；循环内不发外部请求。
- 用完的连接/游标/文件必须关闭（context manager）。

**提交前自查清单（满足全部才算完成）**
- [ ] lint / type / test 全绿（`make check`）
- [ ] 无死代码、无重复、无魔法数字
- [ ] 命名语义化、公共接口有 docstring
- [ ] 无吞异常、错误处理完整、日志留痕
- [ ] 每个行为变更有具体断言的测试
- [ ] 符合依赖方向与拆分标准

---

## 安全红线（不可妥协）

- **不输出诊断/处方**；越界请求一律拒答或降级。
- 高风险信号（胸痛/呼吸困难/卒中征兆）**强制转人工（HITL）**。
- 任何输出必须带**引用溯源 + 免责声明**。
- **PHI 脱敏在入口中间件完成**，进入 LLM/RAG 之前必须已脱敏；日志只记脱敏后文本。
- 密钥只走环境变量（`.env`，不入库），参考 `.env.example`。

## 评测

- 指标口径与基线见 `docs/system-design.md` §9；红队集/拒答集**禁止**进入 prompt、训练或 RAG 语料。
- CI 的 `eval.yml` 手动/带 label 触发，评测报告作为 PR 依据。
